"use strict";
// type intersection(&) usado para extenter propriedades 
// em objectos que não temos acesso, mais atenção para interface e diferente
function handleProdutosCarro(dado) {
    dado.preco;
    dado.rodas;
    dado.portas;
}
function handleInterfaceCarro(dado) { }
window.userId = 200;
