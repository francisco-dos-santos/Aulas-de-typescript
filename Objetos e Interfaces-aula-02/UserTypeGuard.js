"use strict";
/*async function fetchCursos(){
  const response = await fetch('./cursos.json')
  const json= await response.json()
  handleCursos(json)
}*/
//fetchCursos()
/*
function handleCursos(data:unknown){
  if(data instanceof Array){
    console.log("é uma instancia de Array")
  }
  if(Array.isArray(data)){
    console.log('é um Array',data)
  }
}

// a function precisa retornar boolean quando usamos o (is)
//o typescript não executa javascrip , então ele usa o type predicate
function isString(value:unknown): value is string{
  return typeof value ==="string"
}

function handleData(data:unknown){
  if(isString(data)){
    data.toLowerCase()
  }
}

//handleData('Francisco')
*/ 
